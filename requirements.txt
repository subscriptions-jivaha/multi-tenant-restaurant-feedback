# Restaurant Review API - Requirements
# Python 3.9+ required

# Testing Framework (Required)
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional: Data Validation (Choose one based on preference)
# pydantic>=2.5.0        # Modern, recommended for data models
# dataclasses-json>=0.6.0  # If using dataclasses with JSON serialization

# Optional: Code Quality Tools (Recommended but not required)
# black>=23.0.0          # Code formatter
# flake8>=6.0.0          # Linting
# mypy>=1.5.0            # Type checking

# Optional: API Server (Only if doing Bonus Challenge #3)
# flask>=3.0.0
# fastapi>=0.109.0
# uvicorn>=0.27.0        # ASGI server for FastAPI
